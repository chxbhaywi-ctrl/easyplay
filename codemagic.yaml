workflows:
  android-apk:
    name: Android APK Build
    instance_type: mac_mini_m2
    max_build_duration: 60
    environment:
      node: 20
      java: 17
      vars:
        GRADLE_OPTS: "-Xmx3g -XX:MaxMetaspaceSize=512m"
    cache:
      cache_paths:
        - ~/.gradle/caches
        - ~/.gradle/wrapper
        - artifacts/sms-forwarder/node_modules

    scripts:
      - name: Install dependencies
        script: |
          yarn install

      - name: Expo prebuild
        script: |
          cd artifacts/sms-forwarder
          yarn expo prebuild --platform android --clean --no-install

      - name: Set yarn worklets path
        script: |
          WORKLETS=$(find artifacts/sms-forwarder/node_modules -type d -name "react-native-worklets" | grep -v reanimated | head -1)
          sed -i "s|WORKLETS_PATH_PLACEHOLDER|$WORKLETS|g" \
            artifacts/sms-forwarder/android/app/build.gradle || true

      - name: Build release APK
        script: |
          cd artifacts/sms-forwarder/android
          chmod +x gradlew
          ./gradlew assembleRelease --no-daemon --quiet

    artifacts:
      - artifacts/sms-forwarder/android/app/build/outputs/apk/release/*.apk

    publishing:
      email:
        recipients:
          - chxbhaywi@gmail.com
